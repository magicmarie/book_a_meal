language: python
python:
  - "3.6.4"
cache: pip3
install:
  - pip3 install -r requirements.txt
  - pip3 install pytest pytest-cov
  - pip3 install coveralls
script:
  - pytest --cov=app/
after_success:
  - coveralls
